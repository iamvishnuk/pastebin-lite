# Redis connection URL - required for paste storage and persistence
# Local: redis://localhost:6379
# Upstash (Vercel recommended): redis://default:password@host:port
REDIS_URL="redis://localhost:6379"

# Base URL for the application (used when generating share links)
NEXT_PUBLIC_BASE_URL="http://localhost:3000"

# Enable test mode for deterministic time-based testing
TEST_MODE=0
